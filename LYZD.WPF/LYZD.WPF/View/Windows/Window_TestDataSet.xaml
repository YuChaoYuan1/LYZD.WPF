<Window x:Class="LYZD.WPF.View.Windows.Window_TestDataSet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LYZD.WPF.View.Windows"
        mc:Ignorable="d"
        Title="Window_TestDataSet" Height="600" Width="1200"
        WindowStartupLocation="CenterScreen"
        FontSize="16"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">
    <Window.Resources>
        <Style TargetType="{x:Type DataGrid}"  >
            <Setter Property="RowHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRowHeader">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridRowHeader">
                                    <Border Width="30"
                                        BorderThickness="1"
                                        BorderBrush="{DynamicResource 边框颜色}"
                                        Background="{DynamicResource 控件有效}">
                                        <ContentPresenter Content="{Binding Index}"
                                                      Margin="2"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border BorderThickness="1"  CornerRadius="10"  Margin="5" UseLayoutRounding="True">
        <Border.Effect>
            <DropShadowEffect Color="Gray"  ShadowDepth="0" BlurRadius="5" Opacity="0.3" Direction="0"></DropShadowEffect>
        </Border.Effect>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0"    MouseMove="Window_MouseMove">
                <Border.Background>
                    <LinearGradientBrush  EndPoint="1,0.5" StartPoint="0,0.5">
                        <GradientStop Color="#257ACD"     Offset="0"/>
                        <GradientStop Color="#48B7FC"  Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBlock Text="参数修改" FontSize="25"  Margin="10 0" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="White"/>
                    <CheckBox Content="统一修改" Name="AllSet" IsChecked="False"  VerticalAlignment="Center" Foreground="White" Margin="80 0"  HorizontalAlignment="Center"/>
                </StackPanel>

            </Border>
            <Grid Grid.Row="0"   Border.MouseLeftButtonDown="StackPanel_MouseLeftButtonDown" HorizontalAlignment="Right" VerticalAlignment="Top" >
                <Grid.Resources>
                    <Style TargetType="Border"   x:Key="StyleBorderClose">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Red"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Resources>
                <Border Style="{StaticResource StyleBorderClose}"   Margin="3,5" >
                    <Image Width="32"  Name="imageClose"  VerticalAlignment="Top" HorizontalAlignment="Right" 
                Source="/Images/Close.png"  />
                </Border>
            </Grid>

            <Grid Grid.Row="1">
                <DataGrid  Name="dataGrid"  CellEditEnding="dataGrid_CellEditEnding"  Style="{StaticResource dataGridStyleDetailResult}"  IsReadOnly="False"  ItemsSource="{Binding  CheckResults}"  >
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="{DynamicResource 要检}" Width="auto"
                                                Binding="{Binding 要检}"
                                                IsReadOnly="True"
						                        CanUserResize="False">
                            <DataGridCheckBoxColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGridCheckBoxColumn.CellStyle>
                        </DataGridCheckBoxColumn>
                    </DataGrid.Columns>

                </DataGrid>
            </Grid>

            <Button HorizontalAlignment="Right"  Margin="15"   Grid.Row=" 1"    ToolTip="保存所有的配置"
                  VerticalAlignment="Bottom"
                  CommandParameter="SetResoultData"
                  Command="{Binding LocalCommand}"
                    >
                <Image Width="40" Source="/Images/Save.png"/>
            </Button>
        </Grid>
    </Border>
</Window>
