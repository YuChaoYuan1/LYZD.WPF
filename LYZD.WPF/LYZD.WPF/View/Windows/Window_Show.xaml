<Window x:Class="LYZD.WPF.View.Windows.Window_Show"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LYZD.WPF.View.Windows"
        mc:Ignorable="d"
        Title="Window_Show" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True" 
        Background="Transparent"
        MaxWidth="{StaticResource {x:Static SystemParameters.MaximizedPrimaryScreenWidthKey}}" 
        MaxHeight="{StaticResource {x:Static SystemParameters.MaximizedPrimaryScreenHeightKey}}">
    <Window.Effect>
        <DropShadowEffect Color="Gray"  ShadowDepth="0" BlurRadius="5" Opacity="1" Direction="0.5"/>
    </Window.Effect>
    <Border BorderThickness="1"  CornerRadius="10"  Margin="2" UseLayoutRounding="True" >
        <!--<Border.Effect>
            <DropShadowEffect Color="Gray"  ShadowDepth="0" BlurRadius="5" Opacity="1" Direction="0.5"/>
        </Border.Effect>-->
        <Grid Background="{DynamicResource 窗口背景色}"  >

            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid   Background="#00027B"  Name="TopGrid" MouseMove="UserControl_MouseMove">
                <StackPanel Orientation="Horizontal"  VerticalAlignment="Center"  HorizontalAlignment="Left" >
                    <Image x:Name="Icon" Width="24" Height="24" Margin="5 0 5 0"/>
                    <TextBlock Name="txt"  VerticalAlignment="Center" Foreground="White" FontSize="14"/>
                </StackPanel>

                <StackPanel  HorizontalAlignment="Right"  
                    Orientation="Horizontal"
                    Grid.Row="0">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="Button"   x:Key="StyleBorderClose">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Red"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Resources>
                    <Button Background="Transparent" Style="{Binding}" BorderThickness="0" Click="click_Max">
                        <Image Width="24" Name="imageMax" VerticalAlignment="Top"  Source="/Images/Reduction.png" />
                    </Button>
                    <Button Background="Transparent" Style="{Binding StyleBorderClose}" BorderThickness="0" Click="click_close">
                        <Image Width="24"  Name="imageClose"  VerticalAlignment="Top" Source="/Images/Close.png"/>
                    </Button>
                    <!--<Border Margin="3,5" Name="borderMax">
                    <Image Width="24" Name="imageMax"
                   VerticalAlignment="Top"  Source="/Images/Reduction.png" />
                </Border>
                <Border Style="{StaticResource StyleBorderClose}"   Margin="3,5" >
                    <Image Width="24"  Name="imageClose"  VerticalAlignment="Top"
                Source="/Images/Close.png"  />
                </Border>-->
                </StackPanel>
            </Grid>

            <Grid  Name="grid"  Grid.Row="1">
            </Grid>
        </Grid>
    </Border>

</Window>
