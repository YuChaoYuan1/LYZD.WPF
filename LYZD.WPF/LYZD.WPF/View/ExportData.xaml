<basemodel:DockControlDisposable  
            xmlns:Model="clr-namespace:LYZD.WPF.Model" 
            xmlns:CheckInfo="clr-namespace:LYZD.ViewModel.CheckInfo;assembly=LYZD.ViewModel"
            xmlns:basemodel="clr-namespace:LYZD.WPF.Model" x:Class="LYZD.WPF.View.ExportData"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LYZD.WPF.View" xmlns:ViewModel="clr-namespace:LYZD.ViewModel.Meters;assembly=LYZD.ViewModel" xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero" xmlns:local1="clr-namespace:LYZD.WPF.Controls"
    mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <basemodel:DockControlDisposable.Resources>
        <ViewModel:MetersViewModel x:Key="MetersViewModel"/>
        <ContextMenu x:Key="contextMenu"
                     MenuItem.Click="ContextMenu_Click"
               ItemsSource="{Binding FilterCollection, Source={StaticResource MetersViewModel}}">
            <ContextMenu.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ColumnName, StringFormat=\{0\}}"
                                   VerticalAlignment="Center"
                                   Margin="3"/>
                        <TextBlock Text="{Binding CompareExpression}"
                                   VerticalAlignment="Center"
                                   Margin="3"/>
                        <TextBlock Text="{Binding ValueDisplay}"
                                   VerticalAlignment="Center"
                                   Margin="3"/>
                    </StackPanel>
                </DataTemplate>
            </ContextMenu.ItemTemplate>
        </ContextMenu>
        <Style x:Key="DataGridRowHeaderStyle1" TargetType="{x:Type DataGridRowHeader}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridRowHeader}">
                        <Grid>
                            <Themes:DataGridHeaderBorder BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" IsPressed="{TemplateBinding IsPressed}" IsHovered="{TemplateBinding IsMouseOver}" IsSelected="{TemplateBinding IsRowSelected}" Orientation="Horizontal" Padding="{TemplateBinding Padding}" SeparatorBrush="{TemplateBinding SeparatorBrush}" SeparatorVisibility="{TemplateBinding SeparatorVisibility}">
                                <Grid Width="30">
                                    <CheckBox IsChecked="{Binding IsSelected,Mode=TwoWay}"
                                              Click="CheckBox_Click"
                                              HorizontalAlignment="Center"
                                              Margin="3"
                                              VerticalAlignment="Center"/>
                                </Grid>
                            </Themes:DataGridHeaderBorder>
                            <Thumb x:Name="PART_TopHeaderGripper" VerticalAlignment="Top">
                                <Thumb.Style>
                                    <Style TargetType="{x:Type Thumb}">
                                        <Setter Property="Height" Value="8"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Cursor" Value="SizeNS"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Thumb.Style>
                            </Thumb>
                            <Thumb x:Name="PART_BottomHeaderGripper" VerticalAlignment="Bottom">
                                <Thumb.Style>
                                    <Style TargetType="{x:Type Thumb}">
                                        <Setter Property="Height" Value="8"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Cursor" Value="SizeNS"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Thumb.Style>
                            </Thumb>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </basemodel:DockControlDisposable.Resources>
    <Grid>
        <TabControl   Background="{DynamicResource 窗口背景色}">
            <TabItem Header="表信息"     Foreground="{DynamicResource 字体颜色标准}"     Width="300" >
                <Grid  DataContext="{StaticResource MetersViewModel}">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <DataGrid  x:Name="datagridMeter"    	Grid.Row="0"
			                     CanUserReorderColumns="True"
			                     Margin="3,0,3,3"
                                 MouseRightButtonDown="datagridMeter_MouseRightButtonDown"
			                     HeadersVisibility="All"
			                     Style="{StaticResource dataGridStyleDetailResult}"
			                     ItemsSource="{Binding Meters}"
			                     SelectionUnit="FullRow"
                                 SelectionChanged="datagridMeter_SelectionChanged"
                                 RowHeaderStyle="{DynamicResource DataGridRowHeaderStyle1}"/>
                   
                    <local1:DataPager Margin="10,3,5,3"  DataContext="{Binding PagerModel}" Grid.Row="1"  Width="400" HorizontalAlignment="Center"  VerticalAlignment="Center"/>
                    <Button Content="导出数据" Width="80" Height="25" Grid.Row="1" HorizontalAlignment="Right" Foreground="White" FontSize="14"  Background="Green"  Style="{Binding }" Click="btn_SaveData"/>
                    <Button Content="上传数据" Width="80" Height="25" Grid.Row="1" HorizontalAlignment="Right" Foreground="White" FontSize="14"  Background="Green"  Style="{Binding }" Click="btn_UpData" Margin=" 0 0 90 0"/>
                    <StackPanel  Orientation="Horizontal"  HorizontalAlignment="Left" Grid.Row="1">
                        <ListBox ItemsSource="{Binding SearchItems}"
                         Background="{x:Null}"
                         BorderBrush="{x:Null}">
                            <ListBox.Resources>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <StackPanel Orientation="Horizontal"   Margin="3">
                                                    <CheckBox VerticalAlignment="Center"   Margin="3"   IsChecked="{Binding IsSelected,Mode=TwoWay}"  Visibility="Collapsed"/>
                                                    <ComboBox Width="120" Margin="3"   ItemsSource="{Binding ColumnNames}"
                                                                       Style="{DynamicResource StyleComboBox}" 
                                                                       SelectedValue="{Binding ColumnName,Mode=TwoWay}" />
                                                    <ComboBox Width="80" Margin="3"
                                                              Style="{DynamicResource StyleComboBox}"
                                                              ItemsSource="{Binding Conditions}" 
                                                              SelectedItem="{Binding CompareCondition, Mode=TwoWay}"/>
                                                    <ComboBox Width="200" Margin="3"
                                                              Style="{DynamicResource StyleComboBox}"
                                                              ItemsSource="{Binding SeachValues}" 
                                                              Text="{Binding SearchValue, Mode=TwoWay}"
                                                          IsEditable="True"/>
                                                </StackPanel>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.Resources>
                        </ListBox>
                        <Button Content="查 询" Margin="5" 
                                Height="25"
                                Width="80"
                                Click="Click_SearchMeters"
                                HorizontalAlignment="Center" 
                                Foreground="White" FontSize="14"  Background="Blue"  Style="{Binding }"/>
                    </StackPanel>

                </Grid>
                <!--ContextMenu="{StaticResource contextMenu}"-->
    
            </TabItem>
            <TabItem Header="详细结论"     Foreground="{DynamicResource 字体颜色标准}"     Width="300">
                <ScrollViewer Grid.Column="0"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  Margin="3,0">
                    <StackPanel Name="resultContainer"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</basemodel:DockControlDisposable>
