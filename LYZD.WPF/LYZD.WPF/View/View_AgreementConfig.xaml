<basemodel:DockControlDisposable  
             xmlns:basemodel="clr-namespace:LYZD.WPF.Model" 
             x:Class="LYZD.WPF.View.View_AgreementConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LYZD.WPF.View" xmlns:Controls1="clr-namespace:LYZD.WPF.Controls"
             mc:Ignorable="d" 
              Loaded="DockControlDisposable_Loaded"
             Foreground="{DynamicResource 字体颜色标准}"
             d:DesignHeight="600" d:DesignWidth="1200">
    <basemodel:DockControlDisposable.Resources>
        <XmlDataProvider x:Key="ProviderMeterProtocol"
                         Source="/LYZD.WPF;component/ConfigData/AgreementConfig.xml"/>
        <XmlDataProvider x:Key="ProviderCarrierProtocol"
                         Source="/LYZD.WPF;component/ConfigData/CarrierConfig.xml"/>
    </basemodel:DockControlDisposable.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderThickness="0 0 5 0" BorderBrush="{DynamicResource 窗口背景深色}" />

        <Grid Grid.Column="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>


            <Grid Grid.Column="0"  DataContext="{Binding Source={StaticResource ProviderMeterProtocol}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Button   Click="Add_Click"    Grid.Row="0"      HorizontalAlignment="Left" VerticalAlignment="Top"   ToolTip="添加新协议"   >
                    <Image Width="18" Source="/Images/Add.png"/>
                </Button>

                <TextBlock Text="协议配置" HorizontalAlignment="Center"/>


                <ListBox x:Name="listBox" Margin="1"  Width="160"  Grid.Row="1"
                         BorderBrush="{DynamicResource 边框颜色}"
                         Background="{DynamicResource 窗口背景色}"
                         DataContext="{Binding XPath=/DgnProtocol/Protocols}" 
				         ItemsSource="{Binding XPath=R}"
                         SelectedIndex="0"
                         HorizontalAlignment="Left" SelectionChanged="listBox_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" >
                                <Controls1:EditBox Text="{Binding XPath=@Name,Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center" 
                                           Width="125"/>
                                <Button   HorizontalAlignment="Right"   Click="Del_Click"  Margin="3"  Name="btn_Del"
                                      ToolTip="删除当前协议"  VerticalAlignment="Center"  >
                                    <Image Width="18" Source="/Images/Del.png"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>
            </Grid>

            <StackPanel Grid.Column="1" HorizontalAlignment="Left" Margin="10 20 0 0"  DataContext="{Binding SelectedItem, ElementName=listBox}">
                <StackPanel.Resources>
                    <Style TargetType="ListBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBox">
                                    <StackPanel IsItemsHost="True"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="ItemTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <StackPanel Margin="3"
                                Orientation="Horizontal">
                                        <TextBlock Width="70"
                                           TextAlignment="Left"
                                           VerticalAlignment="Center"
                                           Margin="3" Text="{Binding StringFormat=\{0\}:, XPath=@Display}"/>
                                        <TextBox Width="130" Text="{Binding XPath=@Password,Mode=TwoWay}"/>
                                        <TextBlock Text="等级:"
                                           Margin="25,3,3,3"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right"
                                           Width="30"/>
                                        <TextBox Width="60"
                                         VerticalAlignment="Center" Text="{Binding XPath=@Class,Mode=TwoWay}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </StackPanel.Resources>

                <StackPanel Orientation="Horizontal">
                    <CheckBox  x:Name="checkBox" Content="有编程建"  Width="80"   Margin="3"
                          HorizontalAlignment="Right"  IsChecked="{Binding XPath=HaveProgrammingkey,Mode=TwoWay}"/>
                    <CheckBox Margin="3"  Width="120"   HorizontalAlignment="Right"   Content="数据域包含密码" IsChecked="{Binding XPath=DataFieldPassword,Mode=TwoWay}"/>
                    <CheckBox Margin="3"    Width="120"    HorizontalAlignment="Right"  Content="写块操作加AA" IsChecked="{Binding XPath=BlockAddAA,Mode=TwoWay}"/>
                </StackPanel>


                <StackPanel  Orientation="Horizontal"  HorizontalAlignment="Right"    Margin="3">
                    <TextBlock Text="通信参数:"    Margin="3" Width="60"/>
                    <Controls1:ControlEnumComboBox    Width="100"    EnumName="SerialPortPara"  Style="{DynamicResource StyleComboBox}"
                                                      SelectedValue="{Binding XPath=Setting,Mode=TwoWay}"/>
                    <TextBlock Text="登录用户:" Margin="3"  VerticalAlignment="Center" Width="60"/>
                    <TextBox Width="100" Text="{Binding XPath=UserID,Mode=TwoWay}"/>
                </StackPanel>

                <StackPanel Margin="3"     Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="验证方式:"   Margin="3" VerticalAlignment="Center"  Width="60"/>
                    <Controls1:ControlEnumComboBox  Width="100"   Style="{DynamicResource StyleComboBox}" VerticalAlignment="Center"  
                                                    EnumName="Authentication" SelectedIndex="{Binding XPath=VerifyPasswordType,Mode=TwoWay}"/>
                    <TextBlock Text="唤醒符数:"  Margin="3" TextWrapping="WrapWithOverflow"  VerticalAlignment="Center"  Width="60"/>
                    <TextBox   Width="100"    VerticalAlignment="Center" Text="{Binding XPath=FECount,Mode=TwoWay}"/>
                </StackPanel>
                <StackPanel Margin="3"  Orientation="Horizontal"   HorizontalAlignment="Right">
                    <TextBlock Text="日期格式:"    Margin="3"    VerticalAlignment="Center" Width="60"/>
                    <Controls1:ControlEnumComboBox Width="100"    Style="{DynamicResource StyleComboBox}"  EnumName="TimeFormat" 
                                                   SelectedValue="{Binding XPath=DateTimeFormat,Mode=TwoWay}"   ToolTip="{Binding SelectedValue, RelativeSource={RelativeSource Self}}"/>
                    <TextBlock Text="周日格式:"  Margin="3"
                               VerticalAlignment="Center"  Width="60"/>
                    <Controls1:ControlEnumComboBox  Width="100"  Style="{DynamicResource StyleComboBox}" 
                                                    EnumName="SundayId" SelectedValue="{Binding XPath=SundayIndex,Mode=TwoWay}"/>
                </StackPanel>
                <StackPanel Margin="3,10,3,10" Orientation="Horizontal">
                    <TextBlock Text="尖:"
                               Margin="10,3,3,3"
                               VerticalAlignment="Center"
                               Width="20"/>
                    <Controls1:ControlEnumComboBox   Width="50"  EnumName="TimeSlot"
                                                     Style="{DynamicResource StyleComboBox}" SelectedValue="{Binding XPath=FeiLvId/@Jian,Mode=TwoWay}"/>
                    <TextBlock Text="峰:"
                               Margin="10,3,3,3"
                               VerticalAlignment="Center"
                               Width="20"/>
                    <Controls1:ControlEnumComboBox  Width="50"   EnumName="TimeSlot" 
                                                    Style="{DynamicResource StyleComboBox}" SelectedValue="{Binding XPath=FeiLvId/@Feng,Mode=TwoWay}"/>
                    <TextBlock Text="平:"
                               Margin="10,3,3,3"
                               VerticalAlignment="Center"
                               Width="20"/>
                    <Controls1:ControlEnumComboBox  Width="50"   EnumName="TimeSlot" 
                                                    Style="{DynamicResource StyleComboBox}" SelectedValue="{Binding XPath=FeiLvId/@Ping,Mode=TwoWay}"/>
                    <TextBlock Text="谷:"
                               Margin="10,3,3,3"
                               VerticalAlignment="Center"
                               Width="20"/>
                    <Controls1:ControlEnumComboBox   Width="50"   EnumName="TimeSlot" 
                                                     Style="{DynamicResource StyleComboBox}" SelectedValue="{Binding XPath=FeiLvId/@Gu,Mode=TwoWay}"/>
                </StackPanel>
                <!--<StackPanel Margin="3,10,3,10" Orientation="Horizontal">
                    <TextBlock Text="使用协议:"
                               Margin="10,3,3,3"
                               VerticalAlignment="Center"
                               Width="60"/>
                    <Controls1:ControlEnumComboBox   Width="120"  EnumName="MeterProtocolType"
                                                     Style="{DynamicResource StyleComboBox}" SelectedValue="{Binding XPath=ClassName,Mode=TwoWay}"/>
                </StackPanel>-->
                <Grid Height="5"
                      Margin="0,3"
                      HorizontalAlignment="Stretch"
                      Background="{DynamicResource 窗口背景深色}"/>
                <ListBox 
                    ItemsSource="{Binding XPath=OperationsHaveKey/Operation}" Margin="3"
                    Visibility="{Binding IsChecked, Converter={StaticResource BoolToVisibilityConverter}, ElementName=checkBox}"/>
                <ListBox 
                    ItemsSource="{Binding XPath=OperationsNoKey/Operation}"
                    Visibility="{Binding IsChecked, Converter={StaticResource NotBoolToVisibilityConverter}, ElementName=checkBox}"
                    Margin="3,10,3,10"/>
            </StackPanel>


            <Button HorizontalAlignment="Right"
                        Click="btn_Save"
                        ToolTip="保存协议配置信息"
                        VerticalAlignment="Bottom"
                        Grid.Column="1"
                        Margin="10">
                <Image  Width="32"
                        Height="32" 
                        Source="/Images/save.png"/>
            </Button>
        </Grid>


        <Grid Grid.Column="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0"  DataContext="{Binding Source={StaticResource ProviderCarrierProtocol}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="29*"/>
                    <RowDefinition Height="111*"/>
                </Grid.RowDefinitions>
                <Button   Click="ZB_Btn_Add"    Grid.Row="0"      HorizontalAlignment="Left" VerticalAlignment="Top"   ToolTip="添加新协议"   >
                    <Image Width="18" Source="/Images/Add.png"/>
                </Button>

                <TextBlock Text="载波协议配置" HorizontalAlignment="Center"/>
                
                <ListBox x:Name="list_ZB" Margin="1,1,0,1"  Width="160"  Grid.Row="1"
                         BorderBrush="{DynamicResource 边框颜色}"
                         Background="{DynamicResource 窗口背景色}"
                         DataContext="{Binding XPath=/CarrierProtocol/Protocols}" 
				         ItemsSource="{Binding XPath=R}"
                         SelectedIndex="0"
                         HorizontalAlignment="Left" Grid.RowSpan="2">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" >
                                <!--<TextBox Text="{Binding XPath=@Name,Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center"  Width="125"/>-->
                                <Controls1:EditBox Margin="3"	Text="{Binding XPath=@Name,Mode=TwoWay}" HorizontalAlignment="Left"  VerticalAlignment="Center"  Width="120"/>
                                <Button   HorizontalAlignment="Right"   Click="ZB_Btn_Del"  Margin="3"
                                          ToolTip="删除当前协议"  VerticalAlignment="Center"  >
                                    <Image Width="18" Source="/Images/Del.png"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>
            </Grid>


            <StackPanel HorizontalAlignment="Left"  Margin="10 20 0 0" Grid.Column="1"   DataContext="{Binding SelectedItem, ElementName=list_ZB}">
                <StackPanel Orientation="Horizontal">
                    <CheckBox  x:Name="chk_route" Content="路由标识"  Width="80"   Margin="3"
                          HorizontalAlignment="Right"  IsChecked="{Binding XPath=IsRoute,Mode=TwoWay}"/>
                    <CheckBox  x:Name="chk_broadband" Content="宽带载波"  Width="80"   Margin="3"
                          HorizontalAlignment="Right"  IsChecked="{Binding XPath=IsBroad,Mode=TwoWay}"/>
                    <!--<CheckBox 
                      x:Name="chk_ZB" 
                      Content="是否双模"
                      Width="80"
                      Margin="3"
                      HorizontalAlignment="Right"
                      IsChecked="{Binding XPath=IsCheck_SM,Mode=TwoWay}"/>-->
                </StackPanel>                                                                           

                <StackPanel    Orientation="Horizontal" HorizontalAlignment="Right"  Margin="3">
              
                    <TextBlock Text="通信参数:"  Margin="3" Width="60"/>
                    <Controls1:ControlEnumComboBox Width="100"   EnumName="SerialPortPara"
                                                   Style="{DynamicResource StyleComboBox}" 
                                                   SelectedValue="{Binding XPath=BaudRate,Mode=TwoWay}"/>
                    <TextBlock Text="通讯介质:"
                               Margin="3"
                               VerticalAlignment="Center"
                               Width="60"/>
                    <!--ConnectMode-->
                    <!--<TextBox Width="100" Text="{Binding XPath=CarrierType,Mode=TwoWay}"/>-->
                    <Controls1:ControlEnumComboBox Width="100"   EnumName="ConnectMode"
                                                   Style="{DynamicResource StyleComboBox}" 
                                                   SelectedValue="{Binding XPath=CarrierType,Mode=TwoWay}"/>
                </StackPanel>
                <StackPanel Margin="3"     Orientation="Horizontal">
                    <TextBlock Text="抄表器类型:"
                               Margin="3"
                               VerticalAlignment="Center"
                               Width="60"/>
                    <Controls1:ControlEnumComboBox Width="100"  Style="{DynamicResource StyleComboBox}"
                                                   VerticalAlignment="Center"  EnumName="CarrierType" SelectedIndex="{Binding XPath=RdType,Mode=TwoWay}"/>
                    <TextBlock Text="通讯端口:" Margin="3"
                               TextWrapping="WrapWithOverflow" VerticalAlignment="Center"  Width="60"/>
                    <TextBox  Width="100"   VerticalAlignment="Center"  Text="{Binding XPath=Comm,Mode=TwoWay}"/>
                
                </StackPanel>
                <StackPanel Orientation="Horizontal"   Margin="3" >
                    <TextBlock Text="命令延时:" Margin="3" VerticalAlignment="Center"  Width="60"/>
                    <TextBox  Width="100"   VerticalAlignment="Center"  Text="{Binding XPath=CmdTime,Mode=TwoWay}"/>
                    <TextBlock Text="字节延时:" Margin="3" VerticalAlignment="Center"  Width="60"/>
                    <TextBox  Width="100"   VerticalAlignment="Center"  Text="{Binding XPath=ByteTime,Mode=TwoWay}"/>
                </StackPanel>
            </StackPanel>
            <Button HorizontalAlignment="Right"
                        Click="ZB_Btn_Save"
                        ToolTip="保存载波协议配置信息"
                        VerticalAlignment="Bottom"
                        Grid.Column="1"
                        Margin="10">
                <Image  Width="32"
                        Height="32" 
                        Source="/Images/save.png"/>
            </Button>
        </Grid>

    </Grid>
</basemodel:DockControlDisposable> 
                                                                                                    