<basemodel:DockControlDisposable      
    x:Class="LYZD.WPF.View.View_SchemaConfig"
             xmlns:LocalControls="clr-namespace:LYZD.WPF.Controls" 
             xmlns:basemodel="clr-namespace:LYZD.WPF.Model" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LYZD.WPF.Schema"
             xmlns:schemaViewModel="clr-namespace:LYZD.ViewModel.Schema;assembly=LYZD.ViewModel"
             xmlns:Error="clr-namespace:LYZD.WPF.Schema.Error"
             mc:Ignorable="d" 
             FontSize="14"
             Foreground="{DynamicResource 字体颜色标准}"
             d:DesignHeight="700" d:DesignWidth="1200">

    <basemodel:DockControlDisposable.Resources>
        <Style x:Key="DataGridCellStyle1" TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}" 
                                              Content="{TemplateBinding Content}" 
                                              VerticalAlignment ="Center"
                                              Margin="3,1"
                                              ContentStringFormat="{TemplateBinding ContentStringFormat}" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" 
                            Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                    <Setter Property="Foreground" 
                            Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                    <Setter Property="BorderBrush" 
                            Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush"
                            Value="{DynamicResource {ComponentResourceKey ResourceId=FocusBorderBrushKey, TypeInTargetAssembly={x:Type DataGrid}}}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" 
                            Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <schemaViewModel:SchemaViewModel x:Key="SchemaViewModel"/>
        <schemaViewModel:SchemaOperationViewModel x:Key="SchemasViewModel"/>
        <GridViewColumnCollection x:Key="ColumnsSchemaConfig">
            <GridViewColumn  Header="检定点名称"
                             Width="500">
                <GridViewColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" 
                      Margin="{Binding Level,Converter={StaticResource LevelToIndentConverter}}">
                            <ToggleButton x:Name="Expander" 
                      Style="{StaticResource ExpandCollapseToggleStyle}" 
                      IsChecked="{Binding Path=IsExpanded,
                                 RelativeSource={RelativeSource 
                                                 AncestorType=
                                                  {x:Type TreeViewItem}}}"
												  
             HorizontalAlignment="Center"
             VerticalAlignment="Center"
                      ClickMode="Press"/>
                            <TextBlock Text="{Binding Name}"
										ToolTip="{Binding Name}"
										VerticalAlignment="Center"/>
                        </StackPanel>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Path=HasItems,
                               RelativeSource={RelativeSource 
                               AncestorType={x:Type TreeViewItem}}}" 
                     Value="False">
                                <Setter TargetName="Expander"
                  Property="Visibility"
                  Value="Collapsed"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Width="60" 
                            DisplayMemberBinding="{Binding PointCount, StringFormat=(\{0\}项)}"/>
            <GridViewColumn Width="40">
                <GridViewColumn.CellTemplate>
                    <DataTemplate>
                        <Button Click="Button_Click_RemoveNode" 
                                    VerticalAlignment="Center">
                            <Image Width="16" 
                                       Source="/Images/Del.png" />
                        </Button>
                    </DataTemplate>
                </GridViewColumn.CellTemplate>
            </GridViewColumn>
        </GridViewColumnCollection>
        <Style  x:Key="TreeViewStyleSchemaConfig"
            TargetType="TreeView">
            <Style.Resources>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="Padding" Value="0,2"/>
                    <Setter Property="IsExpanded" Value="True"/>
                    <Setter Property="Foreground"
                        Value="{DynamicResource 字体颜色标准}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TreeViewItem}">
                                <StackPanel VerticalAlignment="Center">
                                    <Border Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <GridViewRowPresenter x:Name="PART_Header" 
                                                              Content="{TemplateBinding Header}" 
                                                              Columns="{StaticResource ColumnsSchemaConfig}" >
                                            <GridViewRowPresenter.Resources>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                </Style>
                                            </GridViewRowPresenter.Resources>
                                        </GridViewRowPresenter>
                                    </Border>
                                    <ItemsPresenter x:Name="ItemsHost" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsExpanded"  Value="false">
                                        <Setter TargetName="ItemsHost"   Property="Visibility"  Value="Collapsed"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="HasHeader"    Value="false"/>
                                            <Condition Property="Width"   Value="Auto"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="PART_Header"    Property="MinWidth"   Value="75"/>
                                    </MultiTrigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="HasHeader"     Value="false"/>
                                            <Condition Property="Height"  Value="Auto"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="PART_Header"  Property="MinHeight"  Value="19"/>
                                    </MultiTrigger>
                                    <Trigger Property="IsSelected"      Value="true">
                                        <Setter Property="Foreground"      Value="Blue"/>
                                        <Setter Property="Background" TargetName="Bd" Value="{DynamicResource 窗口背景深色}"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected"      Value="true"/>
                                            <Condition Property="IsSelectionActive"      Value="false"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="Bd"
                                                Property="Background"
                                                Value="{DynamicResource  {x:Static SystemColors.ControlBrushKey}}"/>
                                        <Setter Property="Foreground"   Value="{DynamicResource      {x:Static SystemColors.ControlTextBrushKey}}"/>
                                    </MultiTrigger>
                                    <Trigger Property="IsEnabled"  Value="false">
                                        <Setter Property="Foreground"  Value="{DynamicResource  {x:Static SystemColors.GrayTextBrushKey}}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Background"  Value="{DynamicResource 表行颜色}"/>
                    <Style.Triggers>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter Property="Background"   Value="{DynamicResource 表行颜色1}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter Property="Background" Value="{DynamicResource 线性渐变颜色}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{DynamicResource 窗口背景色}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Style.Resources>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeView}">
                        <ScrollViewer>
                            <Border BorderBrush="{TemplateBinding BorderBrush}"   Background="White"  BorderThickness="{TemplateBinding BorderThickness}">
                                <DockPanel>
                                    <GridViewHeaderRowPresenter    Columns="{StaticResource ColumnsSchemaConfig}"  DockPanel.Dock="Top"/>
                                    <ItemsPresenter/>
                                </DockPanel>
                            </Border>
                        </ScrollViewer>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="Background"   Value="{DynamicResource 窗口背景色}"/>
            <Setter Property="Background"   Value="{DynamicResource 窗口背景色}"/>
        </Style>
    </basemodel:DockControlDisposable.Resources>


    <Grid DataContext="{StaticResource SchemaViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="420"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TreeView Name="treeFramework"
                      Button.Click="ButtonClick_AddNode"
                      Margin="5"
                      BorderBrush="{x:Null}" 
                      Style="{DynamicResource DataGridTreeSchemaConfig}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children, Mode=TwoWay}"/>
                </TreeView.ItemTemplate>
            </TreeView>
            <Grid Grid.Row="1"  Button.Click="Click_SchemaOperation">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <LocalControls:BorderRadiusButton Style="{StaticResource SunButtonStyle}" BorderRadius="5" x:Name="buttonNew"
                        Margin="3" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Height="24"
                        Width="100"
                        Content="{DynamicResource 新建方案}"/>
                <LocalControls:BorderRadiusButton Style="{StaticResource SunButtonStyle}" BorderRadius="5" Grid.Column="1"
                        x:Name="buttonCopy"
                        Margin="3" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Height="24"
                        Width="100"
                        Content="{DynamicResource 复制方案}"/>
                <LocalControls:BorderRadiusButton Style="{StaticResource SunButtonStyle}" BorderRadius="5" 
                         x:Name="buttonRename"
                        Grid.Row="1"
                        Margin="3" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Height="24"
                        Width="100"
                        Content="{DynamicResource 重命名方案}"/>
                <LocalControls:BorderRadiusButton Style="{StaticResource SunButtonStyle}" BorderRadius="5" Grid.Row="1"
                            Grid.Column="1"
                            x:Name="buttonDelete"
                        Margin="3" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Height="24"
                        Width="100"
                        Content="{DynamicResource 删除方案}"/>
            </Grid>
        </Grid>
        <GridSplitter HorizontalAlignment="Right"
                      Width="3"
                      Grid.RowSpan="2"
                      VerticalAlignment="Stretch" 
                      Background="{DynamicResource 分隔条颜色}" 
                      Margin="0,10,0,10"/>
        <Grid Margin="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="1.8*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid  Name="gridSchemas">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource 方案名}"
                               VerticalAlignment="Center" Margin="3"/>
                <ComboBox Margin="3"
                          Grid.Column="1"
                          Name="comboBoxSchemas"
                          ItemsSource="{Binding Schemas}"
                          Style="{DynamicResource StyleComboBox}"
                          >
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding SCHEMA_NAME}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                    <ComboBox.ContextMenu>
                        <ContextMenu MenuItem.Click="Click_SchemaExpand">
                            <MenuItem x:Name="menuExpand" Header="方案设置"/>
                        </ContextMenu>
                    </ComboBox.ContextMenu>
                </ComboBox>
                <LocalControls:BorderRadiusButton Style="{StaticResource SunButtonStyle}" BorderRadius="5" Grid.Column="2"
                        Click="buttonClick_Save"
                        Margin="3" VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Height="24"
                        Width="100"
                        Content="{DynamicResource 保存方案}"/>
                <!--<Button Grid.Column="3"
                        Style="{DynamicResource ButtonStyleBorder}"
                        Click="buttonClick_SortDefault"
                        Margin="3" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Height="24"
                        Width="120"
                        Content="默认排序"/>-->
                <Button Grid.Column="4"
                        Style="{DynamicResource ButtonStyleBorder}"
                        Click="SchemaDown"
                        Margin="3" VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Height="24"
                        Width="120"
                        Content="下载方案测试"
                        Visibility="Collapsed"/>
                <TextBox Grid.Column="4" Width="600" Name="tipsSchemeName" Visibility="Collapsed"/>
            </Grid>
            <Grid Grid.Row="0"  HorizontalAlignment="Right" Width="auto"  Margin=" 0 0 25 0">
                <Button Click="buttonClick_SortDefault"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        ToolTip="恢复方案的默认排序">
                    <Image Width="24"  Source="/Images/Refresh.png" />
                </Button>
            </Grid>
            <GridSplitter VerticalAlignment="Top"
                      Grid.Row="2"
                      Height="3"
                      HorizontalAlignment="Stretch" 
                      Background="{DynamicResource 分隔条颜色}" 
                      Margin="0"/>
            <LocalControls:TreeViewDragDrop Margin="3"
                                            x:Name="treeSchema"
                                            Grid.Row="1"
                                            Style="{StaticResource TreeViewStyleSchemaConfig}"
                                            Drop="Event_NodeMove"
                  SelectedItemChanged="treeSchema_SelectedItemChanged"
                  ItemsSource="{Binding Children}"
                  BorderBrush="{x:Null}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}"/>
                </TreeView.ItemTemplate>
            </LocalControls:TreeViewDragDrop>


            <Grid Grid.Row="2" Name="gridGeneral">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Button Margin="3,3,6,3" 
                            HorizontalAlignment="Left"
                            Click="Button_Click_AddItem">
                    <Image Width="18"  Source="/Images/Add.png"/>
                </Button>
                <local:DataGridParaValue Grid.Row="1" HeadersVisibility="Column"
                                         AutoGenerateColumns="False"
                                         x:Name="dataGridGeneral"
                                         BorderThickness="0,1,0,0"
                                         IsReadOnly="False"
                                         CellEditEnding="dataGridGeneral_CellEditEnding"
                                         ItemsSource="{Binding ParaValuesView, Mode=TwoWay}"
                                         Background="{x:Null}" SelectionMode="Single" 
                                         Style="{StaticResource dataGridStyleGeneral}">
                    <local:DataGridParaValue.Columns>
                        <DataGridTemplateColumn Header="{DynamicResource 删除}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Width="16" Click="Button_Click_RemoveItem"
                                                HorizontalAlignment="Center" 
                                                VerticalAlignment="Center">
                                        <Image Source="/Images/Del.png"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="{DynamicResource 上移}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Width="16" Click="Button_Click_ItemUp"
                                                HorizontalAlignment="Center" 
                                                VerticalAlignment="Center">
                                        <Image Source="/Images/Up.png"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="{DynamicResource 下移}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Width="16"
                                                Click="Button_Click_ItemDown"
                                                HorizontalAlignment="Center" 
                                                VerticalAlignment="Center">
                                        <Image Source="/Images/Up.png" RenderTransformOrigin="0.5,0.5">
                                            <Image.RenderTransform>
                                                <TransformGroup>
                                                    <ScaleTransform ScaleY="-1" ScaleX="1"/>
                                                    <SkewTransform AngleY="0" AngleX="0"/>
                                                    <RotateTransform Angle="0"/>
                                                    <TranslateTransform/>
                                                </TransformGroup>
                                            </Image.RenderTransform>
                                        </Image>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </local:DataGridParaValue.Columns>
                </local:DataGridParaValue>
            </Grid>
            <ScrollViewer Grid.Row="2"
                          Name="scrollViewError"
                          Margin="3,3,6,3"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          Visibility="Collapsed">
                <Error:ControlTreeError Grid.Row="2" x:Name="controlError"/>
            </ScrollViewer>
            <!--<ScrollViewer Grid.Row="2"
                          Name="scrollViewInitialError"
                          Margin="3,3,6,3"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          Visibility="Collapsed">
                <Error:ControlTreeInitialError Grid.Row="2" x:Name="controlInitialError"/>
            </ScrollViewer>-->

            <CheckBox IsChecked="True"
                      Visibility="Collapsed"
                      Grid.Row="2"
                      Margin="60,6,3,3"
                      HorizontalAlignment="Left" VerticalAlignment="Top"
                      Name="checkBoxErrorView"
                      Content="{DynamicResource 快速添加误差点}"/>
            <Button Content="删除重复点"  Grid.Row="2"  Margin="150,6,3,3" Width="80" Height="20"  HorizontalAlignment="Left" VerticalAlignment="Top"
                    Click="btn_RemoveCF" Visibility="Collapsed" />
            <Grid  Grid.Row="2" Margin="40,5,0,5"       Visibility="Collapsed"     Name="ProtocolPanel">
                <TextBox  Width="200" FontSize="12"  Height="19"      HorizontalAlignment="Left" VerticalAlignment="Top" 
                    Style="{Binding}" Name="Protocol_Name"/>
                <Button Content="添 加" Width="50" Height="18"   HorizontalAlignment="Left" VerticalAlignment="Top" Margin="210,1,0,0"  Background="#2ea400"  Style="{Binding }"
                    BorderThickness="0" FontSize ="12" Click="btn_AddProtocol" Foreground="White"/>

            </Grid>

            <!--<CheckBox IsChecked="True"
                      Grid.Row="2"
                      Margin="60,6,3,3"
                      HorizontalAlignment="Left" VerticalAlignment="Top"
                      Name="checkBoxErrorView2"
                      Content="快速添加误差点"/>-->
        </Grid>
    </Grid>

</basemodel:DockControlDisposable>
