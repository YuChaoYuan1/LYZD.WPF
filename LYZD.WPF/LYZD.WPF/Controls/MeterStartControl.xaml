<UserControl x:Class="LYZD.WPF.Controls.MeterStartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LYZD.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="65" d:DesignWidth="52">
    <UserControl.Resources >
        <Style TargetType="TextBlock"  x:Key="TextStyle">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Width" Value="50"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding MeterNo}" Value="01">
                    <Setter Property="Background" Value="Green"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MeterNo}" Value="02">
                    <Setter  Property="Background" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style> 
    </UserControl.Resources>
    <StackPanel Orientation="Vertical" Background="#2e9afe" Width="50" HorizontalAlignment="Left"  Height="65" VerticalAlignment=" top" Margin="1">
        <TextBlock Text="上限位"  >
            <TextBlock.Style >
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{DynamicResource 字体颜色标准}"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="TextAlignment" Value="Center"/>
                    <Setter Property="Width" Value="50"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Motor}" Value="0">
                            <Setter Property="Background" Value="#2e9afe"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Motor}" Value="01">
                            <Setter  Property="Background" Value="Green"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Motor}" Value="02">
                            <Setter  Property="Background" Value="#2e9afe"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <TextBlock Text="挂表"  >
            <TextBlock.Style >
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{DynamicResource 字体颜色标准}"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="TextAlignment" Value="Center"/>
                    <Setter Property="Width" Value="50"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMeter}" Value="true">
                            <Setter Property="Background" Value="Green"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsMeter}" Value="false">
                            <Setter  Property="Background" Value="red"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <TextBlock Text="下限位" >
            <TextBlock.Style >
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{DynamicResource 字体颜色标准}"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="TextAlignment" Value="Center"/>
                    <Setter Property="Width" Value="50"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Motor}" Value="0">
                            <Setter Property="Background" Value="#2e9afe"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Motor}" Value="01">
                            <Setter  Property="Background" Value="#2e9afe"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Motor}" Value="02">
                            <Setter  Property="Background" Value="Green"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <CheckBox  Style="{Binding }" HorizontalAlignment="Left" Margin="6 0 7 0"  Content="{Binding MeterNo,Mode=TwoWay}"  FontSize="10" TextBlock.TextAlignment="Center"  IsChecked="{Binding IsCheck,Mode=TwoWay}" />
    </StackPanel>
</UserControl >