<UserControl x:Class="LYZD.WPF.Schema.Error.ControlTreeInitialError"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LYZD.WPF.Schema.Error" xmlns:LocalControl="clr-namespace:LYZD.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style  TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" 
                    Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="Padding" Value="2,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Bd" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Margin="5,3"
                    CommandParameter="AddCategory"
                    Command="{Binding LocalCommand}">
                <Image Width="18" ToolTip="添加"
                       Source="/Images/Add.png"/>
            </Button>
        </StackPanel>
        <ListBox ItemsSource="{Binding Categories}"
                 BorderBrush="{x:Null}"
                 Background="{x:Null}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Expander IsExpanded="True">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <LocalControl:ControlEnumComboBox 
                                          VerticalAlignment="Center"  Margin="3,0"
                                          SelectedItem="{Binding Fangxiang}"
                                          EnumName="PowerDirection"
                                          Width="120"
                                           Style="{DynamicResource StyleComboBox}" />
                                <LocalControl:ControlEnumComboBox  Margin="3,0"
                                          VerticalAlignment="Center"
                                          SelectedItem="{Binding Component}"
                                          EnumName="PowerElement"
                                          Width="80"
                                          Style="{DynamicResource StyleComboBox}"/>
                                <LocalControl:ControlEnumComboBox  Margin="3,0"
                                          VerticalAlignment="Center"
                                          SelectedItem="{Binding ErrorType}"
                                          EnumName="ErrorType"
                                          Width="100"
                                          Style="{DynamicResource StyleComboBox}"/>
                                <Button Margin="5,3"
                                        Click="Button_Click">
                                    <Image Width="18" ToolTip="删除"   Source="/Images/Del.png"/>
                                </Button>
                            </StackPanel>
                        </Expander.Header>
                        <local:ControlDataGridError Margin="25,3"
                                    FlagLoad="{Binding FlagLoad}"/>
                    </Expander>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</UserControl>
