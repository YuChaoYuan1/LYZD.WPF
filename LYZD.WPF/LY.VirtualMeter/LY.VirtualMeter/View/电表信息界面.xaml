<UserControl x:Class="LY.VirtualMeter.View.电表信息界面"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LY.VirtualMeter.View" xmlns:controls="clr-namespace:LY.VirtualMeter.Assets.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
       
        <ScrollViewer  Margin="0" VerticalScrollBarVisibility="Auto"  >
            <!--<WrapPanel Orientation="Horizontal" Name="panel" />-->
            <ItemsControl  x:Name="itemsControl" ItemsSource="{Binding  MeterObject}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border >
                            <WrapPanel>
                                <controls:MeterData />
                            </WrapPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</UserControl>
